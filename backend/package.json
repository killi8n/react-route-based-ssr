{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "NODE_ENV=production NODE_PATH=dist node -r esm dist",
    "start:dev": "NODE_ENV=development NODE_PATH=dist npx nodemon -r esm --watch dist/ dist --inspect dist",
    "build:watch": "tsc --watch"
  },
  "devDependencies": {
    "@types/express": "^4.16.1",
    "esm": "^3.2.3",
    "tslint": "^5.12.1",
    "tslint-config-airbnb": "^5.11.1",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^3.3.3"
  },
  "dependencies": {
    "express": "^4.16.4"
  }
}
